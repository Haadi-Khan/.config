# name: Iterating over property values of iterable objects
# key: fin
# uuid: fin
# --
for(let ${item} in ${object}) {
 $0
}
\cdot fof
# name: Iterating over property names of iterable objects
# key: fof
# uuid: fof
# --
for(let ${item} of ${object}) {
 $0
}
