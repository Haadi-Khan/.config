# name: Creates and assigns a local variable using state destructing
# key: cs
# uuid: cs
# --
const { $1 } = this.state`(if (eolp) (js-react-redux-yasnippets-semicolon))`
\cdot cte
# name: Console time end wrapper
# key: cte
# uuid: cte
# --
console.timeEnd('$1')`(if (eolp) (js-react-redux-yasnippets-semicolon))`
