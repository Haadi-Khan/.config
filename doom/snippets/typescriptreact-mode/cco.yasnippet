# name: Writes the the number of times that count() has been invoked at the same line and with the same label
# key: cco
# uuid: cco
# --
console.count(${label})`(if (eolp) (js-react-redux-yasnippets-semicolon))`
\cdot cdc
# name: Error boundaries catch errors during rendering, in lifecycle methods, and in constructors of the whole tree below them
# key: cdc
# uuid: cdc
# --
componentDidCatch(error, info) {
 $0
}
