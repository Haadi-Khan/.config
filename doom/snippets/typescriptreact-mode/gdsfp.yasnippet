# name: Invoked right before calling the render method, both on the initial mount and on subsequent updates
# key: gdsfp
# uuid: gdsfp
# --
static getDerivedStateFromProps(props, state) {
 $0
}
\cdot graphql
# name: graphql
# key: graphql
# uuid: graphql
# --
import { compose, graphql } from 'react-apollo'`(if (eolp) (js-react-redux-yasnippets-semicolon))`
\cdot gsbu
# name: Called right before mutations are made (e.g. before the DOM is updated)
# key: gsbu
# uuid: gsbu
# --
getSnapshotBeforeUpdate = (prevProps, prevState) => {
 $0
}
