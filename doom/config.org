#+title: Config
#+author: Haadi Khan
#+property: tangle yes

My config's gotten pretty ugly over the years so I'm taking the opportunity to clean it up.

Main changes:
- fewer features
  - I don't use LaTeX anymore outside of overleaf
  - No more dumb utilities
  - No more dumb org note taking things
- using vertico instead of ivy
- fixed theming issues (manually set font-locks w/ doom-one)
- fix copilot
- fix broken writeroom mode

* Packages
#+begin_src emacs-lisp :tangle packages.el
;; Theming :3
(package! org-modern)
(package! toc-org)
(package! cdlatex)

;; ;; Programming
(package! eglot)
(package! lsp-booster :recipe (:host github :repo "blahgeek/emacs-lsp-booster"))
(package! eglot-booster :recipe (:host github :repo "jdtsmith/eglot-booster"))

(package! eldoc-box)
(package! harpoon)
(package! flycheck-projectile)
(package! flyover)

;; ;; Typst my beloved
(package! typst-ts-mode)
(package! ox-typst)
#+end_src

* UI
** Basics
Doom stuff
#+begin_src emacs-lisp :tangle config.el
(add-to-list 'default-frame-alist '(undecorated-round . t)) ;; no titlebar

(setq doom-font (font-spec :family "RecMonoLinear" :size 16 :weight 'regular)
      doom-variable-pitch-font (font-spec :family "Recursive Sans Linear Static" :size 16 :weight 'regular))
(setq doom-symbol-font (font-spec :family "RecMonoLinear Nerd Font" ))
(setq fancy-splash-image "~/.config/doom/emacs-dash.png")
#+end_src

#+begin_src emacs-lisp :tangle config.el
(setq display-line-numbers-type 'visual)
(setq-default line-spacing 0.12)
(setq-default tab-width 4) ;; 4 width tabs
(setq scroll-margin 8)
(setq mode-require-final-newline nil) ;; Removes snippets adding a new line
(setq treemacs-position 'right)
#+end_src

** Theme
The =atom-one-dark= theme for Emacs doesn't have configuration for vterm/corfu, so I'm just patching =doom-one=
#+begin_src emacs-lisp :tangle config.el
(setq doom-theme 'doom-one)

(after! doom-themes
  (setq doom-one-palette-overrides
        '((bg          . "#282c34")
          (bg-alt      . "#21252b")
          (fg          . "#abb2bf")
          (fg-alt      . "#828997")
          (red         . "#e06c75")
          (dark-red    . "#be5046")
          (green       . "#98c379")
          (teal        . "#56b6c2")
          (yellow      . "#e5c07b")
          (dark-yellow . "#d19a66")
          (blue        . "#61afef")
          (dark-blue   . "#1e6fb4")
          (magenta     . "#c678dd")
          (grey        . "#5c6370")
          (dark-grey   . "#3e4451")
          (light-grey  . "#abb2bf")
          (comment     . "#5c6370")
          (highlight   . "#3e4451"))))

(custom-set-faces!
 '(font-lock-comment-face       :foreground "#5c6370")
 '(font-lock-keyword-face       :foreground "#c678dd")
 '(font-lock-type-face          :foreground "#e5c07b")
 '(font-lock-constant-face      :foreground "#d19a66")
 '(font-lock-function-name-face :foreground "#61afef")
 '(font-lock-variable-name-face :foreground "#e06c75")
 '(font-lock-builtin-face       :foreground "#56b6c2")
 '(font-lock-string-face        :foreground "#98c379")
 )
#+end_src

** Random
Prevent Compilation window from resizing
#+begin_src emacs-lisp :tangle config.el
(add-hook 'compilation-mode-hook
      (lambda ()
        (interactive)
        (set-window-dedicated-p (selected-window) 1)))
#+end_src

* Emacs Utilities
** Dired
UI improvements
#+begin_src emacs-lisp :tangle config.el
(dirvish-override-dired-mode)
#+end_src
** Org
*** Basics
#+begin_src emacs-lisp :tangle config.el
(setq org-directory "~/org/")
#+end_src
*** Appearance
#+begin_src emacs-lisp :tangle config.el
(setq
 ;; Edit settings
 org-auto-align-tags nil
 org-tags-column 0
 org-fold-catch-invisible-edits 'show-and-error
 org-special-ctrl-a/e t
 org-insert-heading-respect-content t

 ;; Org styling, hide markup etc.
 org-hide-emphasis-markers t
 org-pretty-entities t
 org-ellipsis "…"

 ;; Agenda styling
 org-agenda-tags-column 0
 org-agenda-block-separator ?─
 org-agenda-time-grid
 '((daily today require-timed)
   (800 1000 1200 1400 1600 1800 2000)
   " ┄┄┄┄┄ " "┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄")
 org-agenda-current-time-string
 "◀── now ─────────────────────────────────────────────────")
(setq org-modern-star 'replace)

(add-hook 'org-mode-hook #'org-modern-mode)
(add-hook 'org-agenda-finalize-hook #'org-modern-agenda)


(use-package! evil-org
  :config
  (custom-set-faces
    '(org-document-title ((t (:inherit outline-1 :foreground "#61afef" :height 1.25 :weight bold))))
  '(org-document-info ((t (:inherit outline-1 :foreground "#5c6370" :height 1.15))))
   '(org-level-1 ((t (:inherit outline-1 :height 1.15))))
   '(org-level-2 ((t (:inherit outline-2 :height 1.125))))
   '(org-level-3 ((t (:inherit outline-3 :height 1.1))))
   '(org-level-4 ((t (:inherit outline-4 :height 1.075))))
   '(org-level-5 ((t (:inherit outline-5 :height 1.05))))
   )
  )
(setq org-hide-emphasis-markers t) ;; Removes ** around bolded text etc.
#+end_src

*** TOC
#+begin_src emacs-lisp :tangle config.el
(use-package toc-org
  :commands toc-org-enable
  :init (add-hook 'org-mode-hook 'toc-org-enable)
  )
#+end_src
*** Latex Rendering
For some reason (even on emacs-plus) LaTeX embeds look like utter garbage on Mac (and Linux too for that matter). So, we do some elisp magic to make them look great! We render them at 3x the size, and then scale them down
#+begin_src emacs-lisp :tangle config.el
(setq org-preview-latex-default-process 'dvisvgm)
(plist-put org-format-latex-options :scale 3)
(defun my/image-scale-advice (image)
  (let* ((factor (image-property image :scale))
         (new-factor (if factor
                         (/ factor 2.0)
                       0.5)))
    (image--set-property image :scale new-factor)
    image))
(advice-add 'org--create-inline-image :filter-return #'my/image-scale-advice)
(defun my/overlay-scale-advice (beg end image &optional imagetype)
  (mapc (lambda (ov) (if (equal (overlay-get ov 'org-overlay-type) 'org-latex-overlay)
                                (overlay-put ov
                                             'display
                                             (list 'image :type (or (intern imagetype) 'png) :file image :ascent 'center :scale 0.5))))
        (overlays-at beg)))
(advice-add 'org--make-preview-overlay :after #'my/overlay-scale-advice)

;; (setq company-global-modes '(not org-mode)) ;; No more laggy company completion
(setq global-corfu-mode '(not org-mode))
(add-hook 'org-mode 'org-cdlatex-mode)
(with-eval-after-load 'org
    (add-to-list 'org-latex-packages-alist '("" "braket" t)))
#+end_src

** Deft
Better searching through my org files. Will add a function so I can search through a list of premade tags
#+begin_src  emacs-lisp :tangle config.el
(setq deft-directory "~/org")
(setq deft-default-extension "org")
(setq deft-extensions '("org"))
(setq deft-recursive t)
(setq deft-use-filename-as-title nil)
(setq deft-use-filter-string-for-filename t)
(setq deft-file-naming-rules '((noslash . "-")
                                (nospace . "-")
                                (case-fn . downcase)))
(setq deft-text-mode 'org-mode)
#+end_src

* Keybinds
Adding some basic keybinds
** Evil Rebinds
#+begin_src emacs-lisp :tangle config.el
(defun haadi/controlu ()
  (evil-scroll-up 0)
  (evil-scroll-line-to-center (line-number-at-pos))
  )
(defun haadi/controld ()
  (evil-scroll-down 0)
  (evil-scroll-line-to-center (line-number-at-pos))
  )
;; Need to figure out why search commands don't work...
(defun haadi/searchfwd ()
  (evil-search-next)
  (evil-scroll-line-to-center (line-number-at-pos))
  )
(defun haadi/searchprev ()
  (evil-search-previous)
  (evil-scroll-line-to-center (line-number-at-pos))
  )

(map! :map general-override-mode-map
      :n "<C-u>" #'haadi/controlu
      :n "Q" #'evil-fill-and-move
      :desc "Goto mark" :n "M" #'evil-goto-mark
      )
#+end_src
** Common Files
I have a lot of config files I'd like to edit, so I made some maps
#+begin_src emacs-lisp :tangle config.el
(map! :map general-override-mode-map
      :desc "Open Emacs Config" :leader "f x e" #'(lambda () (interactive) (find-file "~/.config/doom/config.org"))
      :desc "Open Hyprland Config" :leader "f x h" #'(lambda () (interactive) (find-file "~/.config/hypr/hyprland.conf"))
      :desc "Open Waybar Config" :leader "f x w" #'(lambda () (interactive) (find-file "~/.config/waybar/config"))
      :desc "Open Discord Config" :leader "f x d" #'(lambda () (interactive) (find-file "~/.config/legcord/themes/midnight.theme.css"))
      :desc "Open zsh Config" :leader "f x z" #'(lambda() (interactive) (find-file "~/.zshrc"))
      )
#+end_src

Now, for some common files
#+begin_src emacs-lisp :tangle config.el
(map! :map general-override-mode-map
      :desc "Open Todo List" :leader "f t" #'(lambda () (interactive) (find-file "~/org/todos.org.gpg"))
      )
#+end_src
** Org
#+begin_src emacs-lisp :tangle config.el
(map! :map org-mode-map :leader :desc "Babel Tangle" :n "m k" #'org-babel-tangle)
(map! :map org-mode-map :leader :desc "Babel Tangle File" :n "m K" #'org-babel-tangle-file)
#+end_src

Better markup commands
#+begin_src emacs-lisp :tangle config.el
(map! :map org-mode-map "s-b" #'(lambda () (interactive) (er/mark-word) (org-emphasize ?\*)))
(map! :map org-mode-map "s-i" #'(lambda () (interactive) (er/mark-word) (org-emphasize ?\/)))
(map! :map org-mode-map "s-l" #'(lambda () (interactive) (er/mark-word) (org-emphasize ?\=)))
(map! :map org-mode-map "s-u" #'(lambda () (interactive) (er/mark-word) (org-emphasize ?\_)))
#+end_src
** Buffers
#+begin_src emacs-lisp :tangle config.el
(map! :leader :desc "List Buffers" :n "b l" #'list-buffers)
(map! :leader :desc "Buffer Word Count" :n "b w" #'count-words)
#+end_src
** Programming
#+begin_src emacs-lisp :tangle config.el
(map! :leader :desc "Format Buffer" :n "c f" #'eglot-format)
(map! :leader :desc "Docs Glance" :n "c h" #'eldoc-box-hover-at-point-mode)
(map! :leader :desc "Show Project Errors" :n "c X" #'flycheck-projectile-list-errors)
#+end_src

Projectile Errors
#+begin_src emacs-lisp :tangle config.el
(map! :map flycheck-projectile-error-list-mode-map :n "j" #'evil-next-line)
(map! :map flycheck-projectile-error-list-mode-map :n "k" #'evil-previous-line)
#+end_src

*** Harpoon
#+begin_src emacs-lisp :tangle config.el
(map! :leader "j c" 'harpoon-clear)
(map! :leader "j m" 'harpoon-toggle-quick-menu)
(map! :leader "j A" 'harpoon-add-file)
(map! :leader "j t" 'harpoon-toggle-file)

(map! :leader "j a" 'harpoon-go-to-1)
(map! :leader "j s" 'harpoon-go-to-2)
(map! :leader "j d" 'harpoon-go-to-3)
(map! :leader "j f" 'harpoon-go-to-4)
(map! :leader "j q" 'harpoon-go-to-5)
(map! :leader "j w" 'harpoon-go-to-6)
(map! :leader "j e" 'harpoon-go-to-7)
(map! :leader "j r" 'harpoon-go-to-8)

(map! :leader "j D a" 'harpoon-delete-1)
(map! :leader "j D s" 'harpoon-delete-2)
(map! :leader "j D d" 'harpoon-delete-3)
(map! :leader "j D f" 'harpoon-delete-4)
(map! :leader "j D q" 'harpoon-delete-5)
(map! :leader "j D w" 'harpoon-delete-6)
(map! :leader "j D e" 'harpoon-delete-7)
(map! :leader "j D r" 'harpoon-delete-8)
(map! :leader "j D ." 'harpoon-delete-item)
#+end_src
** Elfeed
#+begin_src emacs-lisp :tangle config.el
(map! :map elfeed-search-mode-map :desc "Search in Browser" :n "b" #'elfeed-search-browse-url)
(map! :leader :desc "Open Elfeed" :n "o e" #'elfeed)
#+end_src
** Dired
#+begin_src emacs-lisp :tangle config.el
(map! :leader :desc "Open Dired" :n "o -" #'dirvish)
(map! :map dired-mode-map :desc "Parent directory" :n "h" #'dired-up-directory)
(map! :map dired-mode-map :desc "Into directory" :n "l" #'dired-find-file)
#+end_src
* Programming
** Base Config
Some basic config stuff. I added way too much ram for garbage collection, but I have the RAM so I may as well use it to make sure that my editing isn't slow lol
#+begin_src emacs-lisp :tangle config.el
(setq gc-cons-threshold 250000000) ;; 250mb
(setq read-process-output-max (* 8192 1024)) ;; 8mb

(global-corfu-mode)
(setq corfu-sort-function nil)
(corfu-history-mode)
(setq corfu-auto-prefix 1)
(setq corfu-auto-delay 0.01)
(add-hook 'eglot-managed-mode-hook (lambda () (eglot-inlay-hints-mode -1)))
#+end_src
Makes EGlot stupid fast
#+begin_src emacs-lisp :tangle config.el
(use-package eglot-booster
        :after eglot
        :config (eglot-booster-mode))
#+end_src
** Copilot
Built in GitHub Copilot chat
#+begin_src emacs-lisp :tangle config.el
(gptel-make-gh-copilot "Copilot")
(setq gptel-model 'gpt-4.5-preview
      gptel-backend (gptel-make-gh-copilot "Copilot"))
#+end_src

Inline autocomplete (I prefer this to gptel-autocomplete)
#+begin_src emacs-lisp :tangle config.el
;; (use-package! copilot
;;   :hook (prog-mode . copilot-mode)
;;   :bind (:map copilot-completion-map
;;               ("<tab>" . 'copilot-accept-completion)
;;               ("TAB" . 'copilot-accept-completion)
;;               ("C-TAB" . 'copilot-accept-completion-by-word)
;;               ("C-<tab>" . 'copilot-accept-completion-by-word)))
;; (map! :leader :desc "Toggle copilot" :n "c g" #'copilot-mode)
#+end_src
** Lookup Providers
Great tool called lookup which enables searching for code issues straight from emacs
#+begin_src emacs-lisp :tangle config.el
(setq +lookup-provider-url-alist '(
 ("Doom issues" "https://github.com/orgs/doomemacs/projects/2/views/30?filterQuery=%s")
 ("Doom discourse" "https://discourse.doomemacs.org/search?q=%s")
 ("Oogle" "https://ooglester.com/search?q=%s")
 ("Google images" "https://www.google.com/images?q=%s")
 ("Google maps" "https://maps.google.com/maps?q=%s")
 ("Project Gutenberg" "http://www.gutenberg.org/ebooks/search/?query=%s")
 ("DevDocs.io" "https://devdocs.io/#q=%s")
 ("StackOverflow" "https://stackoverflow.com/search?q=%s")
 ("Github" "https://github.com/search?ref=simplesearch&q=%s")
 ("Youtube" "https://youtube.com/results?aq=f&oq=&search_query=%s")
 ("Wolfram alpha" "https://wolframalpha.com/input/?i=%s")
 ("Wikipedia" "https://wikipedia.org/search-redirect.php?language=en&go=Go&search=%s")
 ("MDN" "https://developer.mozilla.org/en-US/search?q=%s")
 ("Internet archive" "https://web.archive.org/web/*/%s")
 ("Sourcegraph" "https://sourcegraph.com/search?q=context:global+%s&patternType=literal")
 ("Rust Docs" "https://doc.rust-lang.org/std/?search=%s"))
 )
#+end_src

** Flyover
Modern looking flymake/check
#+begin_src emacs-lisp :tangle config.el
(add-hook 'flycheck-mode-hook #'flyover-mode)

(setq flyover-levels '(error warning)) ;; options: error, warning, info

(setq flyover-use-theme-colors t)
(setq flyover-debounce-interval 0.2)

(setq flyover-hide-when-cursor-is-on-same-line t)
(setq flyover-hide-checker-name t)
(setq flyover-show-virtual-line nil)
(setq flyover-show-at-eol t)

;; Yes this is very hacky. Yes I wasted 30 minutes of my time fighting with this
;; setting so I asked Chat to make it so the variable can never change. Fight me
(defun my/disable-flycheck-popup-tip-mode (&rest _)
  "Prevent `flycheck-popup-tip-mode' from being enabled."
  (when flycheck-popup-tip-mode
    (flycheck-popup-tip-mode -1)))

(advice-add 'flycheck-popup-tip-mode :after #'my/disable-flycheck-popup-tip-mode)
#+end_src

** Languages
*** Rust
#+begin_src emacs-lisp :tangle config.el
(after! rustic
 (setq rustic-format-on-save nil))
(add-hook! '(rustic-mode-hook rust-ts-mode-hook) #'eglot-ensure)
#+end_src
*** Python
#+begin_src emacs-lisp :tangle config.el
(set-eglot-client! 'python-ts-mode '("pyright-langserver" "--stdio"))
(add-hook! '(python-mode-hook python-ts-mode-hook) #'eglot-ensure)

;; in python-ts-mode, make SPC c f run ruff format. eglot format doesn't use ruff format, so you need to hook it up. Just make it run the terminal command.
(defun my-python-ts-mode-hook ()
  (local-set-key (kbd "SPC c f") (lambda () (interactive)
                                   (let ((default-directory (project-root (project-current))))
                                     (shell-command "ruff format")))))
(add-hook 'python-ts-mode-hook 'my-python-ts-mode-hook)


#+end_src
*** C/C++
LSP Config
#+begin_src emacs-lisp :tangle config.el
(set-eglot-client! 'c-ts-mode '("clangd" "-j=3" "--clang-tidy"))
(add-hook! '(c-mode-hook c-ts-mode-hook c++-mode-hook c++-ts-mode-hook) #'eglot-ensure)

(use-package cpp-mode
  :mode (("\\.cpp\\'" . cpp-mode)
         ("\\.hpp\\'" . cpp-mode)))

(rassq-delete-all 'c++-ts-mode auto-mode-alist)
#+end_src

Tab width of 2 for C++
#+begin_src emacs-lisp :tangle config.el
(defun my-c++-mode-hook ()
  (setq c-basic-offset 2)
  (setq tab-width 2))
(add-hook 'c++-mode-hook 'my-c++-mode-hook)
#+end_src
*** Typst
#+begin_src emacs-lisp :tangle config.el
(set-eglot-client! 'typst-ts-mode '("tinymist"))
(add-hook! 'typst-ts-mode-hook #'eglot-ensure)
#+end_src

For auto expanding snippets
#+begin_src emacs-lisp :tangle config.el
(defun my-yas-try-expanding-auto-snippets ()
  (when yas-minor-mode
    (let ((yas-buffer-local-condition ''(require-snippet-condition . auto)))
      (yas-expand))))
(add-hook 'post-command-hook #'my-yas-try-expanding-auto-snippets)
#+end_src
*** MDX
Extended markdown files that I use for my blog
#+begin_src emacs-lisp :tangle config.el
(use-package markdown-mode
  :mode (("\\.mdx\\'" . markdown-mode)
         ("\\.md\\'" . markdown-mode)))
#+end_src
*** Typescript
Extended markdown files that I use for my blog
#+begin_src emacs-lisp :tangle config.el
(use-package typescript-ts-mode
  :mode (("\\.ts\\'" . typescript-ts-mode)))
(use-package tsx-ts-mode
  :mode (("\\.tsx\\'" . tsx-ts-mode)))
#+end_src

* Misc.
** Argumentative
#+begin_src emacs-lisp :tangle config.el
(load "~/.config/doom/argumentative.el")
#+end_src